public int maxTriple(int[] nums) {
  if(nums.length == 1){
    return nums[0];
  }
  if(nums.length == 2){
    return Math.max(nums[0],nums[1]);
  }
  
  int num = Math.max(nums[nums.length/2],nums[nums.length-1]);
  
  return Math.max(nums[0],num);
  
  
}
